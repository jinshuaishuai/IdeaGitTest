<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jin.mapper.UserInfoMapper">
    <resultMap type="com.jin.entity.domain.UserInfoDo" id="BaseResultMap">
        <result column="id" jdbcType="INTEGER" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="age" jdbcType="INTEGER" property="age"/>
        <result column="phone" jdbcType="VARCHAR" property="phone"/>
        <result column="is_delete" jdbcType="INTEGER" property="isDelete"/>
        <result column="create_time" jdbcType="VARCHAR" property="createTime"/>
        <result column="update_time" jdbcType="VARCHAR" property="updateTime"/>
    </resultMap>
    <cache/>
    <sql id="Base_Columns">
        id,name,age,phone,is_delete,create_time,update_time
    </sql>
    <select id="getUserListByParam" parameterType="com.jin.entity.domain.UserInfoDo" resultMap="BaseResultMap">
        select <include refid="Base_Columns"/> 
        from user_info 
        <where>
            <if test="id != null">
                and id = #{id, jdbcType=INTEGER}
            </if>
            <if test="name != null and name != ''">
                <bind name="nameLike" value="'%' + name + '%'"/>
                and name like #{nameLike, jdbcType=VARCHAR}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone, jdbcType=VARCHAR}
            </if>
        </where>
    </select>
    <select id="getUserInfoById" resultMap="BaseResultMap" parameterType="int" useCache="true">
        select <include refid="Base_Columns"/> from user_info where id = #{id, jdbcType=INTEGER}
    </select>

    <select id="getDynamicQueryResult" resultType="java.util.LinkedHashMap">
        select <include refid="Base_Columns"/>
        from user_info
    </select>
    <insert id="addBatch" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="id">
        insert into user_info1(name, phone, age)
        values
        <foreach collection="list" item="user" separator=",">
            (
             #{user.name, jdbcType=VARCHAR},
             #{user.phone, jdbcType=VARCHAR},
             #{user.age, jdbcType=INTEGER}
            )
        </foreach>
    </insert>

    <insert id="save" parameterType="com.jin.entity.domain.UserInfoDo" flushCache="false">
        insert into user_info
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null and name != ''">
                name,
            </if>
            <if test="phone != null and phone != ''">
                phone,
            </if>
            <if test="age != null">
                age
            </if>
        </trim>
        <trim prefix="values(" suffix=")" suffixOverrides=",">
            <if test="name != null and name != ''">
                #{name, jdbcType=VARCHAR},
            </if>
            <if test="phone != null and phone != ''">
                #{phone, jdbcType=VARCHAR},
            </if>
            <if test="age != null">
                #{age, jdbcType=INTEGER}
            </if>
        </trim>
    </insert>
    <update id="update" parameterType="com.jin.entity.domain.UserInfoDo" flushCache="false">
        update user_info
        <set>
            <if test="name != null and name != ''">
                name = #{name, jdbcType=VARCHAR},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone, jdbcType=VARCHAR},
            </if>
            <if test="age != null">
                age = #{age, jdbcType=INTEGER}
            </if>
        </set>
        <where>
            <if test="id != null">
                and id = #{id, jdbcType=INTEGER}
            </if>
        </where>
    </update>
    <delete id="delete" parameterType="com.jin.entity.domain.UserInfoDo" flushCache="false">
        delete from user_info
        <where>
            <if test="id != null">
                and id = #{id, jdbcType=INTEGER }
            </if>
        </where>
    </delete>
</mapper>